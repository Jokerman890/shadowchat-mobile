# Apps

This directory contains the platform-specific mobile applications for ShadowChat.

Planned structure:
- `ios/` for the native iOS app in Swift / SwiftUI
- `android/` for the native Android app in Kotlin / Jetpack Compose

These apps should share product direction and architecture, while respecting platform-specific behavior and APIs.
